// console.log("journal.js test");          // Test. journal.js is linked to index.html.

const entry = [
    {
        date: "",
        concepts: "",
        entry: "",
        mood: ""
    }
]

const createEntry = (entry) => {
    //Create your own HTML structure for a journal entry
    return `<h1 class = "${style}">${title}</h1>`
}
